import { Open_Sans, Poppins } from "next/font/google";

export const OpenSans = Open_Sans({ subsets: ["latin"] });
export const PoppinsHeading = Poppins({
  subsets: ["latin"],
  weight: ["400", "700", "900"],
});
